import React from 'react';

const Product: React.FC = () => {
  return (
    <div>
      <h1>Product Component</h1>
      {/* Agrega el contenido que deseas mostrar en el componente Product */}
    </div>
  );
};

export default Product;